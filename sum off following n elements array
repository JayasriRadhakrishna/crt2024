sum of following elements in array

#include <stdio.h>
//read n elements into an array and print sum of that elements//
int main()
{
    int n;
    scanf("%d",&n);
    int a[n],i,t;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        
    }
    int s=0;
    for(i=0;i<n;i++)
    {
       s=s+a[i];
       printf("%d ",s);
    }
}
